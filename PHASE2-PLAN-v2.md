# PHASE2-PLAN v2 — KBV 수익화 (트래픽 실측 반영판)

> 2026-08-26 갱신. 기존 PHASE2-PLAN.md를 이 문서로 대체한다.
> 단계 1(등록 마무리·정찰·모니터링)은 완료됨.

---

## 0. 실측으로 확인된 사실 (판단의 근거)

| 지표 | 값 | 해석 |
|---|---|---|
| /mcp HTTP 요청 | 1,250건/일 | **수요 아님** — 대부분 생존확인 봇(UA에 "never invokes tools" 명시) |
| 실제 tool_call | 24h 0건 / 7일 9건 | 9건도 등록 직후 자체·심사 테스트로 추정 |
| 경쟁 서버 | 로컬 설치형(npx), 툴 5개, 유료화 없음 | **우리 우위 = 원격 호스팅·무설정.** 결손 기능은 일괄검증 |

**결론**: 병목은 결제 인프라가 아니라 인지도다. 다만 결제 부착 비용이 낮고(2~3일, 현금 0원, 무료 티어 유지 시 기존 사용자 영향 0)
x402 Bazaar라는 별도 유통 채널을 여는 효과가 있으므로, 결제를 먼저 붙이고 곧바로 홍보로 잇는다.

**성공 기준 재정의**: 매출액이 아니라 ① 유료 호출 1건 성사 ② 홍보 후 1주간 tool_call 20건 이상.

---

## 1. 작업 순서

| # | 작업 | 예상 | 완료 기준 |
|---|---|---|---|
| 1 | 일괄검증 툴 `check_korean_business_batch` | 1일 | 100건 한도, 테스트 통과, 배포 |
| 2 | REST 엔드포인트 + x402 결제 부착 | 2~3일 | 테스트넷 검증 → 메인넷 실결제 1건 |
| 3 | x402 Bazaar 등록 | 30분 | 등록·검색 노출 확인 |
| 4 | 빌드 후기 글 배포 (별도 세션, 채팅에서 초안 작성) | 1일 | dev.to 게시 + HN/r/mcp/X 배포 |
| 5 | awesome-mcp-korea PR | 30분 | PR 생성 |
| 6 | 1주 후 tool_call 재측정 | — | MONITORING.md 기준선과 비교 |

각 단계 종료 시 멈추고 사용자 보고. 단계를 앞당겨 진행하지 않는다.

---

## 2. 단계 1 — 일괄검증 툴

- 이름: `check_korean_business_batch`
- 입력: `{ business_numbers: string[] }` — 1~100개. 101개 이상이면 **네트워크 호출 전** 에러
- 출력: 기존 status 스키마의 배열 + 요약(`total`, `active`, `closed`, `suspended`, `not_registered`)
- 국세청 `/status`가 1회 100건을 지원하므로 한 번의 호출로 처리 (건별 반복 호출 금지)
- 캐시: 개별 번호 단위로 기존 캐시 재사용. 캐시 히트분은 API 요청에서 제외
- 정규화·에러·로그 정책은 기존 툴과 동일. 조회 내용은 로그 금지
- README 툴 목록·server.json 갱신

**이 툴을 먼저 하는 이유**: 경쟁 서버에 있고 우리에게 없는 유일한 실질 기능이며, 유료화 시 단가가 가장 높은 상품(공급업체 목록 일괄 검증 = 조달 에이전트의 실사용 패턴).

---

## 3. 단계 2 — REST + x402 결제

### 3-1. REST (Bazaar 발견용, 결제의 전제)
- `GET /v1/business/{brno}/status`
- `POST /v1/business/verify`
- `POST /v1/business/batch`
- MCP 툴과 **동일한 내부 함수를 공유**할 것 (로직 중복 금지)

### 3-2. x402 결제
- **구현 전 최신 문서를 반드시 조사**할 것 — x402 스펙은 v2로 개정됐고 라이브러리·헤더 규격이 자주 바뀐다.
  조사 대상: `x402-mcp`(Vercel), `x402-express`, Coinbase 공식 x402 MCP 예제, x402 Foundation 문서.
  조사 결과와 선택한 라이브러리·버전을 먼저 보고할 것.
- 가격(초기안):
  - status: **$0.02**
  - verify: **$0.05**
  - batch: **$0.02 × 건수** (상한 두지 말 것, 100건이면 $2.00)
- **무료 티어: IP당 10회/일** — 초과분만 402. 기존 무료 사용자 이탈 방지 + 신뢰 확보
- 수취: 환경변수 `X402_PAY_TO_ADDRESS` (Secret Manager 경유). 사용자가 메타마스크에 KBV 전용 계정을 새로 만들어 그 주소만 제공한다.
  **개인키·시드문구는 절대 요구하지 말 것. 서버에 저장하지 않는다.**
- 네트워크: Base. **먼저 테스트넷(Base Sepolia) 전 구간 검증 후** 메인넷 전환.

### 3-3. 검증 순서
1. 미결제 호출 → 402 + 가격 응답
2. 결제 후 재시도 → 정상 응답
3. 무료 티어 10회 통과, 11회째 402
4. 메인넷 소액 실결제 1건 성공 → 단계 2 완료

### 3-4. 문서 갱신
- README 가격표·무료 티어·결제 방법 추가
- 디렉토리 설명 문구를 "free during pilot" → "10 free calls/day, then pay-per-call (x402)"로 갱신
- server.json, Glama/Smithery 설명도 동일하게

---

## 4. 단계 3 — x402 Bazaar 등록
- 등록 절차 조사 후 REST 엔드포인트 등록
- 등록 후 검색 노출 확인, 결과 보고

## 5. 단계 5 — awesome-mcp-korea PR
- 포크 → README의 공공데이터 섹션에 한 줄 추가 → 사용자 명의 PR

---

## 6. 원칙 (변경 없음)

- 비밀정보(국세청 키, 지갑 개인키) 하드코딩·커밋·로그 금지
- 조회 내용(사업자번호·대표자명)은 로그·외부 전송 금지, 집계만
- 원천 응답을 그대로 통과시키지 말고 영문 정규화 스키마로 변환
- 스펙이 최근 바뀌었을 가능성을 항상 의심하고 공식 문서 먼저 확인
- 삭제·강제 덮어쓰기·자격증명 접근은 사용자 승인 후 실행
- 단계마다 초보자 눈높이의 한국어 설명

## 7. 이번 범위 밖

- 2호 서버(부동산 등) 신규 개발 — 단계 6 재측정 후 결정
- 주소·좌표 툴, 랜딩 페이지, llms.txt
- mcp.so 유료 제출($39) — 이슈 제출로 이미 대기 중, 재제출 금지
